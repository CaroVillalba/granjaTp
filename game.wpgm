import wollok.game.*
import granja.*
import config.*

program granja {
  game.title("Granja")
	game.height(10)
	game.width(10)
	game.boardGround("ground.png")
	config.confifgurarTeclas()


	juego.iniciar()

  game.start()
}